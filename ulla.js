// Example usage of the if statement
const node = {
    // ... other properties
    color: null  // Assuming color is a property of the node object
    // ... other properties
};

// Checking if the color property is null
if (node.color == null) {
    // Code to execute if the color property is null
    console.log("The color property is null.");
} else {
    // Code to execute if the color property is not null
    console.log("The color property is not null.");
}
